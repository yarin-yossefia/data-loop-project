apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-monitoring.fullname" . }}-test-connection"
  labels:
    {{- include "helm-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
